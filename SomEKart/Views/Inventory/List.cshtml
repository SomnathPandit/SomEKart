@model IEnumerable<Item>

@if (@Model.Count() > 0)
{
    foreach (var item in Model)
    {
        <partial name="_ItemCard" model="item"/>
    }
}
else
{
    <div>
        <h1>No items are available in stock!</h1>
    </div>
}
