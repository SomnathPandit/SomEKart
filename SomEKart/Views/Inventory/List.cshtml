@model IEnumerable<Item>

@foreach(var item in Model)
{
    <partial name="_ItemCard" model="item"/>
    @*<div class="col-sm-4 col-lg-4 col-md-4">
        <div class="thumbnail">
            <img src="@item.ImageUrl" alt="ASP.NET" class="img-responsive"/>
            <div class="caption">
                <h3 class="pull-right" > @item.Price.ToString("c")</h3>
                <h3>
                    <a asp-controller="Inventory" asp-action="Details" asp-route-id="@item.Id">@item.Name</a>
                </h3>
                <p>@item.Description</p>
            </div>
        </div>
    </div>*@
}
